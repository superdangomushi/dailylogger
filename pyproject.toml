# Python は Black で整形する。実行は各 Makefile の `make format` / `make format-check`。
# パッケージ定義ではなくツール設定専用のファイル。
[tool.black]
line-length = 100
target-version = ["py310"]
extend-exclude = '''
/(
  \.venv
  | app/src/main/cpp/whisper\.cpp
)/
'''
